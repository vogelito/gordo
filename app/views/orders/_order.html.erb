<li>
  <span class="address"><%= order.address %></span>
  <span class="timestamp">
    Placed <%= time_ago_in_words(order.created_at) %> ago.
  </span>
  <%- #TODO: orders shouldn't be allowed to be deleted -%>
  <%= render 'shared/order_delete', order: order %>
</li>